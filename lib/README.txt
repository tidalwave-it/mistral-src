These libraries are NOT always required for Mistral, but are needed for compiling it.
Since the most recent versions aren't available in any public Maven repository, it is
necessary to manually install them. Please refer to README.txt in the main directory.
